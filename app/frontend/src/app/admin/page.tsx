import { AdminDashboard } from "@/components/admin/admin-dashboard";

export const metadata = { title: "Panel — Coffee Vibes" };

export default function AdminHomePage() {
  return <AdminDashboard />;
}
